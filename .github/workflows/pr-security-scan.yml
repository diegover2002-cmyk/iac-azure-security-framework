name: PR Security Scan

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  security-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run a dummy security scan
        run: echo "Security scan placeholder. Replace with real scan steps."
